{
  "name": "cloudx-frontend",
  "version": "1.0.0",
  "private": true,
  "description": "cloudx 前端 — React 18 + Ant Design 5 + Vite（后期加入）",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  }
}
